default: &defaults
  name: "blog.subvisual.co"
  enable_front_end_error_catching: true

development:
  <<: *defaults
  active: true

production:
  <<: *defaults
  active: true

staging:
  <<: *defaults
  active: true
