.u-largeMargin
.PostWidthConstrainer
  .ContentFormatter.ContentFormatter--centerHorizontally
    h1.Heading = user.name
    .u-smallMargin
    = image_tag current_user.decorate.photo_url, alt: current_user.decorate.name, class: 'UserForm-picture'
    .u-smallMargin

  = render 'form', user: user
